[pytest]
pythonpath = .
testpaths = tests

norecursedirs =
    .git
    .venv
    venv
    __pycache__
    scripts
    tests/e2e

addopts = -ra

markers =
    unit: fast tests with no external services
    integration: tests requiring external services
    e2e: end-to-end tests requiring Azure resources